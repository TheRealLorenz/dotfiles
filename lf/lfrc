# vim:ft=conf

# Options
set drawbox on
set icons
set shell bash
set shellopts '-eu'

# Custom commands
cmd trash %trash-put $fx
cmd delete trash
cmd append $[[ $1 == *"/" ]] && mkdir -p $1 || touch $1 

# Key bindings
map <delete> delete
map a push :append<space>
